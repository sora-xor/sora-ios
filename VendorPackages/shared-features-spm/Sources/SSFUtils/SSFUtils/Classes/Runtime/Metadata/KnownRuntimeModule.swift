import Foundation

public enum KnowRuntimeModule {
    public enum Utitlity {
        public static let name = "Utility"
        public static let batch = "batch"
        public static let batchAll = "batch_all"
    }
}
