[package]
name = "sora-sr25519-safe-validator"
version = "0.1.0"
edition = "2018"
license = "BSD-3-Clause"
publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
# Cargo.lock pins the exact 2020 dependency graph used by the legacy signer.
# Build with Rust nightly-2020-06-27 (rustc 7750c3d46), matching sr25519lib.
schnorrkel = "=0.9.1"
